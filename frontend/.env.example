# Set empty to use the Vite dev proxy (recommended — avoids CORS when the dev port changes).
# Use http://localhost:8000 only if calling the API directly without the proxy.
VITE_API_URL=
VITE_ENV=development

# Optional: Feature Flags
VITE_ENABLE_ANALYTICS=false
